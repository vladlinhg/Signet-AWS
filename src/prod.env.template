# Production Settings
DEBUG=False
SECRET_KEY=change-me-to-a-secure-random-string-in-production
ALLOWED_HOSTS=your-ec2-ip,localhost,127.0.0.1

# Database (SQLite for MVP, switch to Postgres url later if needed)
# DATABASE_URL=postgres://...
